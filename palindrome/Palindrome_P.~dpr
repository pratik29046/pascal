program Palindrome_P;

uses
  ExceptionLog,
  Forms,
  palindrome_U in 'palindrome_U.pas' {FRM_Palindrome};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TFRM_Palindrome, FRM_Palindrome);
  Application.Run;
end.
